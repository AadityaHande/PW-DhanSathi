name: savings
type: contract

source_dir: smart_contracts

build:
  command: algokit compile python smart_contracts/savings_vault.py

deploy:
  command: python smart_contracts/deploy.py
  environment_secrets:
    - DEPLOYER_MNEMONIC
